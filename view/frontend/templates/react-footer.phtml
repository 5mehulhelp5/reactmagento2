<?php
$inlineJs = false;
if(isset($_GET['js-inline'])) {
    $inlineJs = true;
}
?>
<!-- core js -->
<?php 
if($this->removeAdobeJSJunk()) {
    if(!$inlineJs) {
        if($this->isMinifyEnabled(true)) {
            $fileName = $block->getViewFileUrl('React_React::js/react-core.min.js');
        } else {
            $fileName = $block->getViewFileUrl('React_React::js/react-core.js');
        }
        ?>
        <script defer src="<?php echo $fileName; ?>"></script>
        <?php
    } else {
        echo $this->getInlineJs('react-core.js');
    }
} else {
?>
</style>
<script>
    document.addEventListener('DOMContentLoaded', function() {
    const navToggle = document.querySelector(".action.nav-toggle");
        const navMenu = document.querySelector(".nav-sections");
        // Toggle menu on click
        navToggle.addEventListener("click", function () {
            navMenu.classList.toggle("active");
        });
        // Close menu when clicking outside
        document.addEventListener("click", function (event) {
            if (!navMenu.contains(event.target) && event.target !== navToggle) {
                navMenu.classList.remove("active");
        }
        });
        
    });
</script>
<?php
}
?>
<!-- inline js -->
<?php
if(!isset($_GET['js-junk']) || $_GET['js-junk'] !== 'false') {
    include('cart-js.phtml');
}
?>